# Copy to .env, fill in real values, then: chmod 600 .env
# .env is git-ignored — never commit real secrets.

DB_NAME=pr6gs_startertheme
DB_USER=pr6gs_starterthe
DB_PASSWORD=changeme-generate-a-strong-random-password
DB_ROOT_PASSWORD=changeme-generate-a-different-strong-random-password

# This site's table prefix is the WP default 'wp_' — set in the migrated
# wp-config.php directly (this stack uses the migrated file as-is rather
# than the official image's env-driven config generation), not here.

# SMTP relay (via msmtp — see Dockerfile.php) for outbound WordPress email.
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASSWORD=changeme
SMTP_FROM=mail@startertheme.benoitchantre.com
